import { Canvas, Meta, Title } from "@storybook/addon-docs";
import { defaultDocParams } from "../../../lib/utils";

import QuizContainer from "../../../components/QuizContainer";
import QuestionGenerator from "../../../lib/QuestionGenerator";
import DivisionWithRem from "../../../equation/DivisionWithRem";
const questionGenerator = new QuestionGenerator({
	id: "grade-two_within-10k_division-rem_two-numbers",
	maxNum: 9999,
	EquationClass: DivisionWithRem
});

<Meta title={questionGenerator.getName()} parameters={defaultDocParams()} />

<Title>{questionGenerator.getTitle()}</Title>

<Canvas className="custom-docs-story">
	<QuizContainer questionGenerator={questionGenerator} />
</Canvas>
